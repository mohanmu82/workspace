package com.mycompany.batch.appcatalog;

/**
 * One entry of a select control's static option list — the value that goes into the use case input
 * and the text the operator picks it by.
 */
public record AppPageOption(String key, String value) {}
