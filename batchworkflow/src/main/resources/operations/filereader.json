  {
    "name": "filereader",
    "mandatoryProperties": [
      { "property": "inputFilePath", "dataType": "string" }
      ,{ "property": "inputSource", "dataType": "string", "defaultValue": "FILE", "readOnly": true }
],
    "alias": [
      {
        "name": "DATADIRFILE",
        "mandatoryProperties": [
          { "property": "inputFilePath", "dataType": "string", "defaultValue": "${DATADIR}/${FILENAME}" }
          ,{ "property": "inputSource", "dataType": "string", "defaultValue": "FILE", "readOnly": true }
          ,{ "property": "FILENAME", "dataType": "string", "defaultValue": "" }
        ],        
        "request": {
        }
      }
    ]
  }